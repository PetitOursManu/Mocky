# dependencies
node_modules

# build output
dist
dist-ssr

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
*.local
.env
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local config (keep launch.json, ignore local settings)
.claude/settings.local.json

# Backend data (accounts + per-user projects) — never commit user data
server/data/
