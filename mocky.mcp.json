{
  "$comment": "Mocky Muse — local MCP servers spawned by the backend (stdio). All local, free, OSS. See docs/adr/001-muse.md. Swap or add servers here without touching code: the McpToolRouter matches semantic 'role's to whichever server declares a matching tool.",
  "mcpServers": {
    "fetcher": {
      "command": "npx",
      "args": ["-y", "fetcher-mcp@0.2.1"],
      "autoStart": false,
      "role": "inspiration-fetch",
      "idleTimeoutMs": 300000
    }
  }
}
